spring:
  profiles:
    active: prod
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:~/h2test
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      port: 8888
      enabled: true
      path: /h2